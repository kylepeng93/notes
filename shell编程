1.shell内部变量的引用方式：
	$0 : 代表该脚本的文件名
	$1 : 第一个参数
	$2 : 第二个参数
	$3 : 第三个参数
	.
	.	（依次类推）注意，当参数个数大于等于10个时，需要使用{}将数字包起来
	.
	$# : 参数的总个数
	$@ : 列出所有的参数
	$? : 上一个命令执行的状态，如果命令输入正确且执行成功则返回0,如果输入了
	错误的命令，则返回127;再如果命令的执行结果出错，则返回1。
2.shell中定义数组
	和其他编程语言不用，shell中只能定义一维数组。
